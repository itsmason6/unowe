/* Empty = use this same website for sockets (local Node).
   On Vercel, set this to your Railway/Fly socket host, no trailing slash.
   Example: "wss://unowe-server.up.railway.app" */
window.UNOWE_SOCKET = "";
